{
  "name": "js-app",
  "version": "1.0.0",
  "description": "Minimal Node.js app with a static frontend",
  "scripts": {
    "start": "node src/index.js",
    "build": "mkdir -p dist && cp -r src dist/ && cp -r public dist/",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "eslint": "^8.40.0"
  },
  "keywords": []
}